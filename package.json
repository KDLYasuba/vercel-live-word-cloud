{
  "name": "vercel-live-word-cloud",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "local": "vercel dev --local --listen 4000",
    "deploy": "vercel"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3",
    "qrcode-generator": "^2.0.4",
    "vercel": "^54.15.1"
  }
}
